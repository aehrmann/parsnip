# frozen_string_literal: true

RSpec.describe Parsnip do
  it "has a version number" do
    expect(Parsnip::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
